{% extends 'base.html.twig' %}

{% block title %}Lieu index{% endblock %}

{% block body %}
    <div class="container-fluid">
        <h1>Lieu index</h1>

        <table class="table">
            <thead>
                <tr>
                    <th>Id</th>
                    <th>NomLieu</th>
                    <th>Rue</th>
                    <th>Latitude</th>
                    <th>Longitude</th>
                    <th>actions</th>
                </tr>
            </thead>
            <tbody>
            {% for lieu in lieus %}
                <tr>
                    <td>{{ lieu.id }}</td>
                    <td>{{ lieu.nomLieu }}</td>
                    <td>{{ lieu.rue }}</td>
                    <td>{{ lieu.latitude }}</td>
                    <td>{{ lieu.longitude }}</td>
                    <td>
                        <a href="{{ path('app_lieu_show', {'id': lieu.id}) }}">Détails</a>
                        <a href="{{ path('app_lieu_edit', {'id': lieu.id}) }}">Modifier</a>
                    </td>
                </tr>
            {% else %}
                <tr>
                    <td colspan="6">no records found</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>

        <a href="{{ path('app_lieu_new') }}" class="btn btn-primary">Nouveau lieu</a>
    </div>
{% endblock %}
