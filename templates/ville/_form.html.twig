{{ form_start(form) }}

<div class="row">

    <div class="">
        <table>
            <tbody>
                <tr>
                    <th>{{ form_label(form.nomVille) }}</th>
                    <td>{{ form_widget(form.nomVille) }}</td>
                </tr>
                <tr>
                    <th>{{ form_label(form.codePostal) }}</th>
                    <td>{{ form_widget(form.codePostal) }}</td>
                </tr>
            </tbody>
        </table>
        <br>
        <button class="btn btn-primary">{{ button_label|default('Sauvegarder') }}</button>
        <a href="{{ path('sortie_index') }}" class="btn btn-primary">Retour</a>
    </div>
</div>
{{ form_end(form) }}
